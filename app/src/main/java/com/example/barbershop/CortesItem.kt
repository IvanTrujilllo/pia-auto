package com.example.barbershop

class CortesItem {

    var cortes:Int ? = 0
    var name:String ? = null

    constructor(cortes: Int?, name: String?) {
        this.cortes = cortes
        this.name = name
    }
}