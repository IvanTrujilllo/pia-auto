<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.barbershop">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PropActivity"></activity>
        <activity android:name=".ServiciosActivity" />
        <activity android:name=".CancelarActivity" />
        <activity android:name=".AgendarActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".ForgotPassActivity" />
        <activity android:name=".AddAppointmentActivity" />
        <activity android:name=".AppointmentsIndexActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".RegistrarActivity" />
    </application>

</manifest>